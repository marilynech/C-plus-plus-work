// Ask the user for two integers (up to 100 digits), compute, and print their sum.
//waxmana wants to use string to review ascii code 

#include <iostream>
using namespace std; 

int main(){
    //sum up string 1111 with string 2222 we want to get the string the result will be large so we use string
    //want the string 3333 but we do digit by digit addition, '1' + '2' = 49 + 50 = 99
    string num1, num2;
    cout<<"Enter two intergers up to 100 digits"; 
    cin>>num1>>num2;

    //convert character '1' to int 1 subract the difference which is 48 or subrtract the character 0
    //converting between character and integers 

}
