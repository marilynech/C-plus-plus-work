#include <iostream>
#include <ctime>
#include <cmath>
using namespace std; 

int main(){
    cout<<abs(-5)<<endl; //get two numbers subtract them and find the abs value of the result >= 10 to nag user to give right values
    abs(n-m) >= 10;
    if (n > m)
        int r = rand() % (n-m+1) + m;
    else
        int r = rand() % (m-n+1) + n;
//6-1+1 = 6 
//6-2+1 = 5
rand()% (max - min + 1) + min; // will give you what you need when you know the bounds general equation u can use 
//applies if minimum is not 0
}